{
  "name": "graphql-examples",
  "version": "1.0.0",
  "description": "long-live graphql",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development nodemon src/index.js --exec babel-node",
    "build": "cross-env NODE_ENV=production babel src -d dist",
    "serve": "cross-env NODE_ENV=production node dist/index.js"
  },
  "keywords": [],
  "author": "Ben Diuguid",
  "dependencies": {
    "axios": "^0.12.0",
    "express": "^4.14.0",
    "express-graphql": "^0.5.3",
    "graphql": "^0.6.1"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "cross-env": "^1.0.8",
    "nodemon": "^1.9.2"
  }
}
